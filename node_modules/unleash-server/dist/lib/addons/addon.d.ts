export = Addon;
declare class Addon {
    constructor(definition: any, { getLogger }: {
        getLogger: any;
    });
    logger: any;
    _name: any;
    _definition: any;
    get name(): any;
    get definition(): any;
    fetchRetry(url: any, options?: {}, retries?: number, backoff?: number): Promise<any>;
}
