export = DatadogAddon;
declare class DatadogAddon extends Addon {
    constructor(args: any);
    unleashUrl: any;
    handleEvent(event: any, parameters: any): Promise<void>;
    featureLink(event: any): string;
    generateStaleText(event: any): string;
    generateArchivedText(event: any): string;
    generateText(event: any): string;
    getAction(type: any): any;
}
import Addon = require("./addon");
