export = ClientMetricsStore;
declare class ClientMetricsStore extends EventEmitter {
    constructor(metricsDb: any, eventBus: any, getLogger: any, pollInterval?: number);
    logger: any;
    metricsDb: any;
    eventBus: any;
    highestIdSeen: number;
    startTimer: (action: any) => any;
    _init(pollInterval: any): Promise<void>;
    _startPoller(pollInterval: any): void;
    timer: NodeJS.Timeout;
    _fetchNewAndEmit(): void;
    _emitMetrics(metrics: any): void;
    insert(metrics: any): Promise<void>;
    destroy(): void;
}
import { EventEmitter } from "events";
