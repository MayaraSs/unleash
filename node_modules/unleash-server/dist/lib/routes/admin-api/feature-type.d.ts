import { IUnleashServices } from '../../types/services';
import { IUnleashConfig } from '../../types/option';
declare const Controller: any;
export default class FeatureTypeController extends Controller {
    private featureTypeService;
    private logger;
    constructor(config: IUnleashConfig, { featureTypeService }: Pick<IUnleashServices, 'featureTypeService'>);
    getAllFeatureTypes(req: any, res: any): Promise<void>;
}
export {};
