import Controller from '../controller';
import { IUnleashConfig } from '../../types/option';
import { IUnleashServices } from '../../types/services';
declare class UserFeedbackController extends Controller {
    private logger;
    private userFeedbackService;
    constructor(config: IUnleashConfig, { userFeedbackService }: Pick<IUnleashServices, 'userFeedbackService'>);
    private recordFeedback;
    private updateFeedbackSettings;
}
export default UserFeedbackController;
