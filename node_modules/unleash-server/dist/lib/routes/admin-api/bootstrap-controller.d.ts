import { Response } from 'express';
import Controller from '../controller';
import { AuthedRequest } from '../../types/core';
import { IUnleashConfig } from '../../types/option';
import { IUnleashServices } from '../../types/services';
declare class BootstrapController extends Controller {
    private logger;
    private accessService;
    private contextService;
    private emailService;
    private featureTypeService;
    private projectService;
    private strategyService;
    private tagTypeService;
    private versionService;
    constructor(config: IUnleashConfig, { contextService, tagTypeService, strategyService, projectService, accessService, emailService, versionService, featureTypeService, }: Pick<IUnleashServices, 'contextService' | 'tagTypeService' | 'strategyService' | 'projectService' | 'accessService' | 'emailService' | 'versionService' | 'featureTypeService'>);
    bootstrap(req: AuthedRequest, res: Response): Promise<void>;
}
export default BootstrapController;
