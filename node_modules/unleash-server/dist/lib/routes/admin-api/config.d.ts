import { Request, Response } from 'express';
import { IUnleashServices } from '../../types/services';
import { IUnleashConfig } from '../../types/option';
declare const Controller: any;
declare class ConfigController extends Controller {
    constructor(config: IUnleashConfig, { versionService }: Pick<IUnleashServices, 'versionService'>);
    getUIConfig(req: Request, res: Response): Promise<void>;
}
export default ConfigController;
