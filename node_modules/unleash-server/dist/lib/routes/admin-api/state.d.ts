import { Request, Response } from 'express';
import Controller from '../controller';
import { IUnleashConfig } from '../../types/option';
import { IUnleashServices } from '../../types/services';
declare class StateController extends Controller {
    private logger;
    private stateService;
    constructor(config: IUnleashConfig, { stateService }: Pick<IUnleashServices, 'stateService'>);
    import(req: Request, res: Response): Promise<void>;
    export(req: Request, res: Response): Promise<void>;
}
export default StateController;
