import { Request, Response } from 'express';
import { IUnleashConfig } from '../../types/option';
import { IUnleashServices } from '../../types/services';
import Controller from '../controller';
declare class TagController extends Controller {
    private logger;
    private tagService;
    constructor(config: IUnleashConfig, { tagService }: Pick<IUnleashServices, 'tagService'>);
    getTags(req: Request, res: Response): Promise<void>;
    getTagsByType(req: Request, res: Response): Promise<void>;
    getTag(req: Request, res: Response): Promise<void>;
    createTag(req: Request, res: Response): Promise<void>;
    deleteTag(req: Request, res: Response): Promise<void>;
}
export default TagController;
