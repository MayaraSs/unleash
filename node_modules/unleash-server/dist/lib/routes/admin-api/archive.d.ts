import { Request, Response } from 'express';
import { IUnleashConfig } from '../../types/option';
import { IUnleashServices } from '../../types/services';
import Controller from '../controller';
export default class ArchiveController extends Controller {
    private readonly logger;
    private featureService;
    constructor(config: IUnleashConfig, { featureToggleService, }: Pick<IUnleashServices, 'featureToggleService'>);
    getArchivedFeatures(req: any, res: any): Promise<void>;
    deleteFeature(req: Request<any, {
        featureName: string;
    }, any, any>, res: Response): Promise<void>;
    reviveFeatureToggle(req: any, res: any): Promise<void>;
}
