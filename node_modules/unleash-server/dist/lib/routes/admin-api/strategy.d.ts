import { IUnleashConfig } from '../../types/option';
import { IUnleashServices } from '../../types/services';
declare const Controller: any;
declare class StrategyController extends Controller {
    private logger;
    private strategyService;
    constructor(config: IUnleashConfig, { strategyService }: Pick<IUnleashServices, 'strategyService'>);
    getAllStrategies(req: any, res: any): Promise<void>;
    getStrategy(req: any, res: any): Promise<void>;
    removeStrategy(req: any, res: any): Promise<void>;
    createStrategy(req: any, res: any): Promise<void>;
    updateStrategy(req: any, res: any): Promise<void>;
    deprecateStrategy(req: any, res: any): Promise<void>;
    reactivateStrategy(req: any, res: any): Promise<void>;
}
export default StrategyController;
