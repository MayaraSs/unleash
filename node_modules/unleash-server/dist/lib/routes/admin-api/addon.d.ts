import { Request, Response } from 'express';
import Controller from '../controller';
import { IUnleashConfig } from '../../types/option';
import { IUnleashServices } from '../../types/services';
declare class AddonController extends Controller {
    private logger;
    private addonService;
    constructor(config: IUnleashConfig, { addonService }: Pick<IUnleashServices, 'addonService'>);
    getAddons(req: Request, res: Response): Promise<void>;
    getAddon(req: Request, res: Response): Promise<void>;
    updateAddon(req: Request, res: Response): Promise<void>;
    createAddon(req: Request, res: Response): Promise<void>;
    deleteAddon(req: Request, res: Response): Promise<void>;
}
export default AddonController;
