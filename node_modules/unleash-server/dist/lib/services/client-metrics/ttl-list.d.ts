export = TTLList;
declare class TTLList extends EventEmitter {
    constructor({ interval, expireAmount, expireType, }?: {
        interval?: number;
        expireAmount?: number;
        expireType?: string;
    });
    interval: number;
    expireAmount: number;
    expireType: string;
    list: List;
    startTimer(): void;
    timer: NodeJS.Timeout;
    add(value: any, timestamp?: Date): void;
    timedCheck(): void;
    destroy(): void;
}
import { EventEmitter } from "events";
import List = require("./list");
