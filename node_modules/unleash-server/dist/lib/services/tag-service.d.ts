import { ITag } from '../db/tag-store';
export default class TagService {
    private tagStore;
    private eventStore;
    private logger;
    constructor({ tagStore, eventStore }: {
        tagStore: any;
        eventStore: any;
    }, { getLogger }: {
        getLogger: any;
    });
    getTags(): Promise<ITag[]>;
    getTagsByType(type: string): Promise<ITag[]>;
    getTag({ type, value }: ITag): Promise<ITag>;
    validateUnique(tag: ITag): Promise<void>;
    validate(tag: ITag): Promise<ITag>;
    createTag(tag: ITag, userName: string): Promise<void>;
    deleteTag(tag: ITag, userName: string): Promise<void>;
}
