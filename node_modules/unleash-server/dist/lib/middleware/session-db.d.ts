import { IUnleashConfig } from '../types/option';
import { IUnleashStores } from '../types/stores';
declare function sessionDb(config: Pick<IUnleashConfig, 'session' | 'server' | 'secureHeaders'>, stores: Pick<IUnleashStores, 'db'>): any;
export default sessionDb;
