import { IUnleashConfig } from '../types/option';
declare const apiAccessMiddleware: ({ getLogger, authentication, }: Pick<IUnleashConfig, 'getLogger' | 'authentication'>, { apiTokenService }: any) => any;
export default apiAccessMiddleware;
