import UserStore, { IUserLookup } from '../../lib/db/user-store';
import User from '../../lib/types/user';
declare class UserStoreMock extends UserStore {
    data: any[];
    idSeq: number;
    constructor();
    hasUser({ id, username, email, }: IUserLookup): Promise<number | undefined>;
    insert(user: User): Promise<User>;
    update(id: number, user: User): Promise<User>;
    get({ id, username, email }: IUserLookup): Promise<User>;
    getAll(): Promise<User[]>;
    setPasswordHash(userId: number, passwordHash: string): Promise<void>;
    getPasswordHash(id: number): Promise<string>;
    delete(id: number): Promise<void>;
    successfullyLogin(user: User): Promise<void>;
    buildSelectUser(): any;
    search(): Promise<User[]>;
    getAllWithId(): Promise<User[]>;
    incLoginAttempts(): Promise<void>;
}
export default UserStoreMock;
